<view
  wx:if="{{_open}}"
  class="mui mui-backdrop backdrop-transition {{mClass}}"
  style="{{_show ? _endStyle : _startStyle}}{{_transitionStyle}}{{mStyle}}"
  bind:tap="_backdropClick"
>
  <slot></slot>
</view>