<view class="mui-ripple-view" bindtap="ripple">
  <view
    wx:for="{{rippleList}}"
    style="width:{{item.width}}px;height:{{item.width}}px;left:{{item.x}}px;top:{{item.y}}px" class="mui-ripple mui-ripple-animation"
  />
</view>