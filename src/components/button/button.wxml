<mui-button-base
  app-parameter="{{appParameter}}"
  component="button"
  center-ripple="{{(shape === 'circle' && variant === 'text')}}"
  disabled="{{disabled || loading}}"
  disableRipple="{{disableRipple}}"
  hover-class="{{(disabled || loading) ? '' : 'mui-button-hover'}}"
  m-class="mui-button mui-shape-{{shape}} {{muiClasses}}"
  m-style="{{inline ? '' : 'display: flex;'}}{{mStyle}}"
  form-type="{{formType}}"
  open-type="{{openType}}"
  ripple-color="{{_rippleColor}}"
  session-from="{{sessionFrom}}"
  send-message-title="{{sendMessageTitle}}"
  send-message-path="{{sendMessagePath}}"
  send-message-img="{{sendMessageImg}}"
  show-message-card="{{showMessageCard}}"
>
  <mui-icon wx:if="{{loading}}" m-class="mui-button-loading" name="progress-circle" />
  <slot></slot>
  <mui-icon
    wx:if="{{icon}}"
    name="{{icon}}"
    m-class="icon-in-button icon-position-{{iconPosition}}"
    m-style="color:{{iconColor}};font-size:{{iconSize ? iconSize : 'inherit'}}px;"
  />
</mui-button-base>