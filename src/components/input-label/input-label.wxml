<label
  class="mui mui-input-label form-control-{{_formControl}} margin-{{margin}} shrink-{{shrink}} error-{{error}} focus-{{focus}} animated-{{!disableAnimation}} disabled-{{disabled}} {{muiClasses}}"
  style="{{mStyle}}"
>
  <slot />
  <view class="mui-input-label-asterisk">*</view>
</label>