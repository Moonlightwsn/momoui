@import './theme.wxss';

.mui-ripple-base {
  position: relative;
  cursor: pointer;
}

.mui-ripple-container {
  position: absolute;
  overflow: hidden;
  pointer-events: none;
}

.mui-ripple {
  display: inline-block;
  position: absolute;
  opacity: 0;
  border-radius: 100%;
}